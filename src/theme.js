import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1B1D1F',
    },
    secondary: {
      main: '#FF6B0A',
    },
    custom: {
      main: '#a7a3a3',
    },
  },
});

export default theme;
