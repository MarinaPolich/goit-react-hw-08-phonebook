import styled from 'styled-components';
import { ListItemText } from '@mui/material';

export const ListContactItem = styled(ListItemText)({
  '& .MuiListItemText-primary': {
    fontSize: 16,
  },

  '& .MuiListItemText-secondary': {
    fontSize: 16,
  },
});
